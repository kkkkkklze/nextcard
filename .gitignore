# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
# Game run directory - everything inside is regenerated by the game,
# except the gametest structures: Forge reads those from disk at runtime, so they must be committed.
run/*
!run/gameteststructures/
runs
run-data

repo