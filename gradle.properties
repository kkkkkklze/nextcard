# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# JDK 17 location for the Java toolchain. The automatic detection of
# "$GRADLE_USER_HOME/jdks" does not pick this one up (it is nested one level
# deeper than Gradle expects), and toolchain auto-provisioning would download
# from GitHub, which is unreachable here. Change or remove this line if you
# install a JDK 17 somewhere else (e.g. C:/Program Files/Java/jdk-17).
org.gradle.java.installations.paths=C:/Users/Administrator/.gradle/jdks/eclipse_adoptium-17-amd64-windows/jdk-17.0.18+8

# Parchment mappings (optional: only adds readable parameter names while coding).
# Disabled by default because maven.parchmentmc.org redirects its downloads to
# storage.googleapis.com, which is unreachable from mainland China.
#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
#parchment_minecraft_version=1.20.1
#parchment_mappings_version=2023.09.03
# Environment Properties
# You can find the latest versions here: https://files.minecraftforge.net/net/minecraftforge/forge/index_1.20.1.html
# The Minecraft version must agree with the Forge version to get a valid artifact
minecraft_version=1.20.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.20.1, 1.21)
# The Forge version must agree with the Minecraft version to get a valid artifact.
# 47.4.23 is the newest release of the still-maintained 1.20.1 Forge line.
forge_version=47.4.23
# The Forge version range can use any version of Forge as bounds.
# Keep it at [47,) for maximum pack compatibility, or set the lower bound to the
# oldest Forge version you actually test against.
forge_version_range=[47,)
# The loader version range can only use the major version of FML as bounds
loader_version_range=[47,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=nextcard
# The human-readable display name for the mod.
mod_name=NextCard
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=All Rights Reserved
# The mod version. See https://semver.org/
mod_version=0.1.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.klze.nextcard
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=klze
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=Card-drafting framework for modpacks: draw cards from world blocks and build systems with direction, method and mutation cards.

## Publishing
# Project ids on the mod hosting platforms, used by the `publishMods` task.
# Leave empty to skip that platform. Access tokens are NOT stored here - export them instead:
#   CURSEFORGE_TOKEN / MODRINTH_TOKEN, and optionally CHANGELOG for the release notes.
curseforge_project_id=
modrinth_project_id=
