# nextCard 引擎侧进度 · 2026-09-25

**给**：卡池侧（inrralves / cxxx-0v0 / frostx3346-dot）
**仓库**：`https://github.com/kkkkkklze/nextcard`（私有，分支 `master`）
**这份文档回答三个问题**：引擎现在能做到哪、你们现在能怎么参与、以及卡在你们那边的 8 个问题。

---

## 一、引擎现在做到哪了

**已经完成的是"抽卡这件事的全部规则"，还没有接到游戏里。**

| 部分 | 状态 | 说明 |
|---|---|---|
| 抽卡引擎 | ✅ 完成，15 项纯逻辑测试全通过 | 三阶段：先由日程决定这次能出哪些等级 → 再按标签份额决定从哪个池抽 → 池内等概率。全程没有一句针对某张卡的特殊判断 |
| 卡片数据模型 | ✅ 完成 | 一张卡＝等级 + 若干标签 + 种类（方向 / 方式 / 质变）+ 所属体系 + 抽到它的前置要求。**每张卡只能被一个人拥有**（没有"叠两张"的概念） |
| 标签系统 | ✅ 完成 | 标签是一等公民，可以自己声明"只用来做判断、不参与抽卡概率"。前置要求统一写成"拥有任意一张带某标签的卡" |
| 卡池 | ✅ 完成 | 池子是"等级 × 标签"自动投影出来的，**没有任何手工注册口**；空池自动剔除；全空时返回"卡池无卡"而不是报错 |
| 效果系统 | ⚠️ 只有骨架 | 能存、能校验、能算"该加该减"的对账，但**具体有哪些效果写法还没登记**（注册表是空的）。这是下一阶段的主活 |
| 接到游戏里（方块、存档、界面、联机） | ❌ 未开始 | 目前工程里的方块/物品/命令还是模板示例，跟卡牌玩法无关 |
| 示例内容 | 17 张卡 + 4 个标签 | 只为验证引擎能跑，**随时可整体删空**；17 张卡的效果栏全是空的 |

一句话：**地基浇完了，还没盖房子**。引擎现在能算"这 5 格该出什么"，但算出来的效果没法执行，因为它还不认识任何效果写法。

---

## 二、你们交来的卡表，我们读完了，结论是这样

- 卡表正本的真实形态（九栏 txt）我们已经摸清，也确认了《09 组件拆分与接口契约》里那份 JSON 字段表跟内容正本**对不上**——那 11 个字段在卡表全文里一次都没出现过。所以我们不打算按那份 schema 做，改从 txt 出发。**具体见另一份《协作口径对齐》，那边有 8 个问题等回复。**
- inrralves 那个分支（`feat/parry-charge-mechanic-vocabulary`，盾反 + 蓄力的机制词表，13 个文件、11 个测试）方向是对的，**跟内容侧真实需求能对上**：卡表里确实有 22 个具名层数资源、17 种触发时机、一批"上限解除""翻倍""按层折算"的写法。里面留的两处待定已由 klze 拍板：
  - 窗口延长项**只走加算**；"某卡在场则乘算"另开一个独立字段承载，两种算法各占一格互不干扰。
  - 蓄力速度**先按加算**（战意每层 +3% 与器灵苏醒 +20% 合成 +23%），平衡另议。
- **你们的 8 个回复已收到**（2026-09-25），逐条落定与新增的 4 个问题都写在《协作口径对齐》第〇节。影响最大的两条：
  - **入场券保留，只是权重变低** —— 引擎的日程结构不用改，把入场券权重做成可配即可；请回填"降到多少"（P1）。
  - **猎人、盗贼移入待开发** —— 首轮范围从 419 张缩到 **325 张**、三级池从 21 个缩到 **16 个**（去掉埋伏 18 / 循迹 18 / 梁上君子 20 / 怀璧其罪 18 / 瞒天过海 20，合计 94 张）。随之推迟的还有《07》"最难 Top14"里的 6 项：坐庄、李代桃僵、四阶饵、偷取四类、伪装、强制索敌。首轮要重点证明能表达的仍是这 8 个：假身、链与传导、多段刀路、重武蓄力、精准弹反窗口、暗杀处决阈值、潜形三级感知、血勇血池。

---

## 三、接下来按这个顺序做

| 顺序 | 做什么 | 状态 |
|---|---|---|
| 0 | 修自动检查（流水线此前一次都没真正跑过，原因见第四节） | ✅ 已推 |
| 1 | 口径对齐（8 个问题） | ⏳ **等你们回复**，其中 3 条阻塞后续 |
| 2 | 写"卡表 txt → 引擎卡数据"的转换工具（含去重） | 等 Q2、Q3 |
| 3 | 公共底座：数值与乘区、事件、层数账本、判定条件、存档、联机同步、调试命令 | 等 Q5–Q7 |
| 4 | 合并 inrralves 的词表分支 | 顺序 0 完成后即可 |
| 5 | 效果组合方式（三选一 / 条件分支 / 按顺序轮转 / 循环 / 有限递归 / 连锁关系） | 顺序 3 之后 |
| 6 | 接到游戏里：卡数据热加载、抽卡方块、玩家卡库存档、网络、五格界面 | 等 Q1、Q8 |
| 7 | 逐池核对"21 个池 × 效果写法"覆盖表，每格要么给出写法样例，要么列为反例退回内容侧 | 顺序 2+3+5 之后 |

**验收标准**（这条对我们双方都适用）：某类效果"能不能实现"不看谁说了行，看能不能给出一段 JSON 样例并被引擎加载通过。给不出的就是反例，要么补词表、要么改卡面写法。

---

## 四、仓库与工具现状（你们接手要用到的）

- **分支**：`master` 已含纯逻辑引擎 + 示例内容；`feat/parry-charge-mechanic-vocabulary` 是词表首批，还没并进主干（等 CI 绿）。
- **归属**：`CODEOWNERS` 已配，规格文档与构建配置由 klze 审，引擎与内容由你们分工共审。**注意**：截至我们这边最后一次查，`cxxx-0v0` 在 `nextcard` 这个仓的邀请还是待接受状态（另一个仓已经接受了），麻烦去仓库主页看一下顶部的接受提示，或在通知里点接受。
- **自动检查**：此前每次运行都在第一步就失败（仓库里那个启动脚本在 Windows 上提交时没带上"可执行"标记，Linux 机器上跑不了），等于这道门从来没给过任何信号。已修并加了兜底。**这次修复刚推上去，流水线结果我们还没回读到**——如果它仍然红，请把失败在**哪一步的名字**和该步日志尾部贴过来，我们接着修。
- **怎么在本地跑**（需要 JDK 17）：
  - `./gradlew build` 编译 + 检查
  - `./gradlew logicTest` 纯逻辑测试（15 项）
  - `./gradlew runData` 生成资源（生成物要提交）
  - `./gradlew runGameTestServer` 游戏内检查；`./gradlew runClient` 开发客户端
  - **两个已知的坑**：① 工程目录路径里有中文时，标准测试任务会被自动跳过，所以纯逻辑测试请用 `logicTest`（它不受路径影响）；② 依赖走的是国内镜像，遇到 `Connection reset` 先检查是否又去连了官方 Maven。

---

## 五、需要你们回答的事

1. **P2（最阻塞）：引擎怎么知道一张卡是方向卡 / 方式卡 / 质变卡、以及它属于哪个体系？**「类型」「性质」既然是获取方式，这两个信息现在没有出处。三选一：① 另开两栏（建议 `class` 与 `system`）；② 由「三级池」推导（若每个池固定属于一类）；③ 我们按卡面文字推一份清单交你们核对。**这条不定，抽卡日程里"质变卡第 9 抽起才可能出现""某一格必出方向卡"两条规则就没有输入。**
2. **P4：「前置」到底是硬门还是只提权，你们的答复与《00》L192–L195 相反。** 你们这次答"没它就抽不到"（硬门），我们按这个实现；但《00》原文写的是"出现的权重提高""只用于极少数卡""主打仍然是自由选择"。**请把《00》那一段改成硬门的说法**，或列出哪些卡是硬门、哪些只是提权——否则文档和实现会有两份真相。
3. **P1：入场券的权重降到多少？**是"仍然必出现在第一格、后面阶段少出"，还是"跟普通卡一样进概率池"。
4. **P3：419 与我们数出的 410 差在哪 9 张？**给个说法即可（是不是有几张卡的标题行格式漏了）。
5. **撤回一条请求，换成另一条**：我原先要"不截断的效果全文"，理由是《04》汇总表的效果列被硬截在 60 字（419 行里 328 行以省略号结束）。**这条作废**——那份 419 条 × 11 键的机器可读导出（`n/m/p/ph/t/k/e/v/b/f/r`，效果全文只有 13 条带省略号）我们已经找到了。改成请你们：**把这份导出正式提交进仓库**（或按 Q2 交 txt 原文，二选一即可，别两份并行）。
6. **`r`（前置）能成链，请定一下顺序规则**：导出里前置成链（不磨之刃 ← 地动余波 ← 钝击）。既然前置是硬门（你们这次答复），链式硬门意味着**链条根部若不在候选里，整条链永远抽不到**。请定：根部怎么保证出现（提权？保底？还是首抽固定给？）。


