# 分工与交接 · 2026-09-25

> 这份是给**同工作区另一个会话**看的（也可能是人）。目的：把谁做什么钉下来，避免两个人改同一处。

## 一、分工

| 谁 | 负责 | 具体路径 |
|---|---|---|
| **本会话（演示与可视化）** | 对外文档、演示页、卡表数据的导入与核对 | `docs/演示/*`、`docs/协作口径对齐-2026-09-25.md`、`docs/引擎侧进度-2026-09-25.md`、`docs/分工与交接-2026-09-25.md`，以及 `src/test/.../NextCardLogicTest.java` 里我那份回归测试 |
| **另一会话（引擎）** | 抽卡引擎、账本、机制词表、CI 与各类门 | `src/main/java/com/klze/nextcard/core/**`、`sim/**`、`build.gradle`、`.github/**`、`src/test/**`（除上面那一个测试方法） |

**边界承诺**：本会话此后不动 `core/**`、`sim/**`、`build.gradle`、`.github/**`。需要引擎配合时（例如演示要的字段），我在本文件下面「需要引擎侧做的」里写清楚，不自己动手。反过来，引擎侧请别改 `docs/演示/*` 的数据段（那一段是从队友导出机械生成的，手改会失联）。

## 二、本会话刚做的事（已完成，不必复看）

1. **演示页**：`docs/演示/nextCard-引擎显微.html`。单文件无外链，内含 `DrawEngine` 三阶段的逐行移植 + 两份数据集：
   - 示例 17 张（仓库真数据）
   - **真卡表 419 张**（从队友《加点星图》导出内联，原文照搬：卡名/方式/三级池/阶段/附带数值/效果全文/前置/备注），其 `tier`/`tags`/`card_class` 由页面上的 `deriveReal()` 推导，**已标成"推导"**。
   - 页面带卡表原文浏览器（按方式/池/阶段筛 + 全文搜索）、逐格概率显微镜、(等级×标签) 投影矩阵、S 曲线、门 G1「把内容删空」开关、以及 6 条现场自检门。
2. **同步了你们的 T5 修复**：`9e0901f` 之后我把演示的移植一起改了（日程内等级被抽干 → 本格空着并留痕）。复验：`示例 17 张 死局 0｜重复 0｜T5 早现 0｜空格 804`、`真卡表 419 死局 0｜T5 早现 0｜空格 0｜最大 S 68.1%`。
3. **交了回归测试**：`NextCardLogicTest.scheduleLawHoldsUnderFiveSlots` —— 你们那次 T5 修复**当时没带测试**，这条补上（确定夹具：2 张 T1 + 1 张 T5、五格 profile、`Map.of(1,100)`，断言 T5 不出现）。它在这棵树上已从红转绿。
4. **两份外发文档**（给卡池侧队友的，无本机路径、无凭据、术语口语化）：口径对齐 Q1–Q8 + P1–P5；引擎侧进度说明。

## 三、需要引擎侧做的（按优先级）

1. **`DrawEngine.java:93` 的注释与新行为矛盾**：第 104–110 行改成"本格空着"之后，注释里那句「权重全零/缺失 → 有候选等级间均匀」已经不再成立（该分支现在只会走 log+continue）。要么改注释，要么把"日程完全没给 `tier_weights`"与"给了但本抽内被抽干"分成两种情形——**注意这是语义分歧**：`DrawSchedule.Row` 的 `tier_weights` 是 optionalFieldOf，缺省为空表，而现在空表＝本格全空。建议保留"空表→均匀"、只在"给了权重但都用尽"时空格。
2. **`weightedPick` 的均匀分支现在可能已不可达**（同上）。按铁律，删掉不可达的第二条路，或者让它真的可达。
3. **五格形态的 G3**：`sim/Scenario` 的 profile 仍只有 1~2 格，而线上内容是 5 格 —— 这次 T5 泄漏正是这个覆盖率洞暴露出来的。请把蒙特卡洛的参考场景改成与 `draw_profiles/*.json` 同形（或直接读真 profile）。
4. **CounterStore 与用户裁定的对齐**（裁定：周期由引擎驱动，但**卡的周期必须由卡自己的 JSON 声明**，字段暂定名 `period`，单位 tick）：请核 CounterStore 有没有声明域，以及"同 tick 内多触发不乱序、不重复"是否成立。
5. `suiteAudit` 我看到你们在做（`TestSuiteAuditTest.java` 在飞）——正好是演示里那条注入实验要的洞（**未登记的测试类会静默不跑，实测 200 局照报 26 通过 0 失败**）。这条我不动，交给你们。

## 四、数据事实（本会话核过，供引擎侧与内容侧共用）

- 卡表 419 张：方式分布 后发先至 91 / 一器通神 85 / 狂噬不休 92 / 鬼影幢幢 149，另有 **快攻 1 张、散人 1 张**——这两张其实是入场券（未开发方式的占位）。21 个三级池张数与《07 落地难度评估》逐格吻合。
- 猎人 + 盗贼 = 埋伏 18 + 循迹 18 + 梁上君子 20 + 怀璧其罪 18 + 瞒天过海 20 = **94 张**（内容侧已答"移入待开发"）→ 首轮范围 **325 张 / 16 池**。
- 导出里 `r` 字段就是**前置**（7 条非空），且**前置能成链**：钝击←钝刃反打、地动余波←钝击、不磨之刃←地动余波。引擎侧请确认 `requires` 要不要支持传递闭合（我建议不做传递，只按字面一层，链由内容自己声明）。
- 语言文件目前只有 3 个键，**419 张卡与 17 张示例卡都没有本地化名**——将来 `GeneratedCoverageTest` 那类门要盯这个。

## 五、还没定的（等用户/内容侧）

- 演示里派生的 `tier`/`tags`/`card_class` 是**我的推导规则**，不是内容侧定义；等内容侧补真字段（对齐文档的 P5/P2）后整段替换 `deriveReal()`。
- 卡表原文（txt 正本）仍未进仓；现在仓库里多一份 419 张的 JSON 快照（在演示 HTML 内），**它是快照不是真本**，别当权威用。
- CI 那次运行的结论还没回读到（本机取凭据被凭据闸门拦、浏览器对 GitHub 是登出态）。
